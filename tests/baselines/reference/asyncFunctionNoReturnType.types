=== tests/cases/compiler/asyncFunctionNoReturnType.ts ===
async () => {
>async () => {    if (window)        return;} : () => Promise<无值>

    if (window)
>window : 任意
>类型别名 :=> 任意

        return;
}

