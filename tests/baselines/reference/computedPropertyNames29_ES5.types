=== tests/cases/conformance/es6/computedProperties/computedPropertyNames29_ES5.ts ===
class C {
>C : C

    bar() {
>bar : () => 数字

        () => {
>() => {            var obj = {                [this.bar()]() { } // needs capture            };        } : () => 无值

            var obj = {
>obj : { [x: 数字]: () => 无值; }
>{                [this.bar()]() { } // needs capture            } : { [x: 数字]: () => 无值; }

                [this.bar()]() { } // needs capture
>this.bar() : 数字
>类型别名 :=> 数字
>this.bar : () => 数字
>this : 本体
>bar : () => 数字

            };
        }
        return 0;
>0 : 0
    }
}
