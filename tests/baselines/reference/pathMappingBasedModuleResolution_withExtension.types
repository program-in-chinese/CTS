=== /a.ts ===
import { foo } from "foo";
>foo : () => 无值

import { bar } from "bar";
>bar : () => 无值

=== /foo/foo.ts ===
export function foo() {}
>foo : () => 无值

=== /bar/bar.js ===
export function bar() {}
>bar : () => 无值

